client
dev tun

remote ${IP} ${PORT} udp

resolv-retry infinite
nobind
persist-key
persist-tun

<ca>
${CA}
</ca>
<cert>
${CERT}
</cert>
<key>
${KEY}
<tls-auth>
${TA}
</tls-auth>

key-direction 1

remote-cert-tls server
cipher AES-256-CBC
auth sha256

verb 3
